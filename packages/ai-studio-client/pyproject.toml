[project]
name = "ai-studio-client"
version = "0.1.0"
description = "Client-side SDK boundary for AI Studio."
requires-python = ">=3.11"
dependencies = [
    "ai-studio-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ai_studio_client"]
