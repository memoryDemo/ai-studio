[project]
name = "ai-studio-core"
version = "0.1.0"
description = "Core contracts for AI Studio."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ai_studio_core"]
